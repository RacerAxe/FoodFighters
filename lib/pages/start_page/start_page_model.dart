import '/flutter_flow/flutter_flow_util.dart';
import 'start_page_widget.dart' show StartPageWidget;
import 'package:flutter/material.dart';

/// Model class for the Start Page, extending FlutterFlowModel.
///
/// This class manages the state and lifecycle of the StartPageWidget.
class StartPageModel extends FlutterFlowModel<StartPageWidget> {
  /// Initializes the model.
  ///
  /// This method is called when the widget is inserted into the widget tree.
  /// It can be used to perform any necessary setup or initialization.
  @override
  void initState(BuildContext context) {
    // Add any initialization logic here
  }

  /// Disposes of the model's resources.
  ///
  /// This method is called when the widget is removed from the widget tree.
  /// It should be used to clean up any resources that were allocated in initState.
  @override
  void dispose() {
    // Add any cleanup logic here
  }
}
